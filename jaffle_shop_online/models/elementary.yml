version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              config:
                severity: warn
              meta:
                description: The column should contain only unique values
      - name: order_id
        data_tests:
          - unique:
              config:
                severity: warn
              meta:
                description: The column should contain only unique values
