version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              config:
                tags:
                  - Marketing
                error_if: '>500'
                warn_if: '>100'
              meta:
                description: The column should contain only unique values
      - name: order_id
        data_tests:
          - unique:
              config:
                tags:
                  - Marketing
                error_if: '>500'
                warn_if: '>100'
              meta:
                description: The column should contain only unique values
