version: 2
models:
  - name: order_items
    columns:
      - name: order_id
        data_tests:
          - unique:
              config:
                tags:
                  - pii
                severity: warn
              meta:
                owner:
                  - joost@elementary-data.com
                description: The column should contain only unique values
      - name: customer_id
        data_tests:
          - unique:
              config:
                tags:
                  - pii
                severity: warn
              meta:
                owner:
                  - joost@elementary-data.com
                description: The column should contain only unique values
