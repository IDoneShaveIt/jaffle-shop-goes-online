version: 2
models:
  - name: order_items
    columns:
      - name: customer_id
        data_tests:
          - unique:
              config:
                tags:
                  - sales
                error_if: '>10000'
                warn_if: '>1000'
              meta:
                owner:
                  - idan+999@elementary-data.com
                description: The column should contain only unique values
      - name: order_id
        data_tests:
          - unique:
              config:
                tags:
                  - sales
                error_if: '>10000'
                warn_if: '>1000'
              meta:
                owner:
                  - idan+999@elementary-data.com
                description: The column should contain only unique values
